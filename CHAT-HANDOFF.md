# ملخص وتسليم شات تطوير mahmoud.jp

آخر تحديث: 2026-07-26

## الهدف

تطوير موقع `mahmoud.jp` محليًا داخل نسخة تجريبية مستقلة، ومراجعة النتيجة قبل
أي رفع إلى GitHub أو نشر على الإنترنت.

## ابدأ من هنا في أي شات جديد

اطلب من Codex:

> اقرأ `CHAT-HANDOFF.md` و`AGENTS.md` بالكامل، ثم شغّل النسخة المحلية وكمل
> تطوير الموقع داخل `M:\PROJECTS\mahmoud.jp-experiment` فقط. لا ترفع أو تنشر
> أي شيء قبل موافقتي.

## المسارات والفروع

- نسخة التجربة: `M:\PROJECTS\mahmoud.jp-experiment`
- النسخة الأصلية غير المعدلة: `M:\PROJECTS\mahmoud.jp`
- فرع التجربة: `experiment/site-v2`
- أول نسخة تصميم جديدة: `2647075`
- آخر تعديلات توسيع هوية الموقع: `fc7d2ec`
- لم يتم Push أو Deploy لهذه النسخة.

## ما تم تنفيذه

- إعادة تصميم الصفحة الرئيسية بالكامل بهوية تقنية داكنة.
- الموقع يعمل بالإنجليزية واليابانية والعربية مع دعم RTL.
- الاحتفاظ بأربعة أزرار في الـHero:
  - Contact
  - Learn more
  - Request CV
  - LinkedIn
- تحويل التركيز من DTP Master فقط إلى منظومة تطبيقات متنوعة.
- إنشاء صورة Hero جديدة تمثل تطبيقات اللغات والتعلم والسحابة والإنتاجية
  والبرمجة.
- إزالة صورة محمود الشخصية من الصفحة الرئيسية.
- إزالة شعار حرف `M` من شريط التنقل.
- إضافة أقسام: المشاريع، نبذة، القدرات، الرحلة المهنية، الكتابة والتواصل.
- إزالة رقم الهاتف العام والإبقاء على البريد وLinkedIn ونموذج التواصل.
- تحديث صورة DTP Master بلقطة حقيقية من واجهة أحدث نسخة تطوير `v1.5.0`.
- إضافة صورة مشاركة اجتماعية مخصصة في `public/og.png`.
- تحسين العرض على الكمبيوتر والموبايل واختبار العربية RTL.

## أهم الملفات الحالية

- الصفحة الجديدة: `src/components/HomeV2.tsx`
- شريط التنقل: `src/components/Navbar.tsx`
- التنسيق: `src/app/globals.css`
- بيانات الموقع والـSEO: `src/app/layout.tsx` و`src/lib/site.ts`
- صورة منظومة التطبيقات: `public/hero-app-ecosystem.png`
- لقطة DTP Master الحديثة: `public/dtp-master-v15-latest.png`
- صورة المشاركة: `public/og.png`

## تشغيل الموقع محليًا

العنوان المحلي الثابت:

`http://127.0.0.1:3001`

- Windows: افتح `OPEN-LOCAL-SITE.cmd`.
- macOS: افتح `open-local-site.command`.
- أول تشغيل فقط قد يثبت المكتبات تلقائيًا باستخدام `npm ci`.
- يلزم تثبيت Node.js LTS وnpm على الجهاز.
- اترك نافذة السيرفر مفتوحة أثناء مشاهدة الموقع، وأغلقها لإيقافه.

## حالة الاختبارات

- Production build: ناجح.
- TypeScript: ناجح.
- ESLint: لا توجد أخطاء.
- توجد ثلاثة تحذيرات قديمة فقط داخل ملفات Demo/Nihongo غير مرتبطة بالتصميم.
- لا يوجد تمدد أفقي في العرض الذي تم اختباره.

## قواعد مهمة للشات القادم

- كل التعديلات تكون داخل نسخة التجربة فقط.
- لا Push ولا Deploy بدون طلب صريح.
- لا تقلل أزرار الـHero عن أربعة.
- لا تعيد صورة محمود أو شعار `M` إلا بطلب صريح.
- لا تجعل DTP Master هو محور الـHero؛ هو مشروع مهم ضمن عدة تطبيقات.
- قبل أي تعديل جديد، راجع الموقع محليًا ثم نفّذ التغيير واختبر البناء.

## الخطوة التالية المقترحة

مراجعة الصفحة الحالية بصريًا، ثم تحديد التعديلات المطلوبة على:

1. أحجام ومسافات الـHero.
2. ترتيب المشاريع وإضافة لقطات حقيقية لباقي التطبيقات.
3. صفحة Projects وصفحات كل تطبيق.
4. كتابة ونشر أول مقال فعلي بدل بطاقات Coming Soon.

